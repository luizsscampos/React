{
  "name": "petshop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pm2 start --env development -i max",
    "start": "yarn && next build && pm2 start --env production -i max"
  },
  "dependencies": {

    
    "axios": "^0.20.0",
    "next": "9.5.3",
    "next-routes": "^1.4.2",
    "react": "16.13.1",
    "react-dom": "16.13.1"
  }
}
